#include<bits/stdc++.h>
//set or unset or toggle the n 
using namespace std;
int main(){
    int n,k;
    cin>>n>>k;
    int mask=(1<<k);
    //toggle the kth bit
    n = n ^ mask;
    //kth bit as set bit
    n = n | mask;
    //kth bit as unset bit
    n = n & ~(mask);
}
